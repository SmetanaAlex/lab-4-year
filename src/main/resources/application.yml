spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/bakery
    username: alex
    password: password
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: update
